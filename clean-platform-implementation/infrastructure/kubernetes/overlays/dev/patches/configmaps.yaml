apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  # Development specific configurations
  api.rate.limit: "1000"
  api.timeout: "60"
  database.pool.size: "5"
  cache.enabled: "false"
  monitoring.verbose: "true"